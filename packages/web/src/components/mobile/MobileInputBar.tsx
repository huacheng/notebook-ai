import { InputBar } from '../shared/InputBar';

/**
 * Mobile input bar - uses the shared InputBar component with mobile styling.
 */
export function MobileInputBar() {
  return <InputBar mobile />;
}
